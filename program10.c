#include<stdio.h>

int main(){
    int a, b;
    
    printf("enter the value of a and b ");
    scanf("%d, %d", &a, &b );
    int area= a*b;
    printf("area of rectangle is %d", area);
    return 0;
}